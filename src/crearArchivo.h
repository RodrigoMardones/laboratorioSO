#include "crearArchivo.c"

/**
 * @brief trims a string
 * 
 * @param s 
 * @return char* 
 */
char* trim(char *s);

/**
 * @brief creates a csv file from a matrix
 * 
 * @param matrix 
 * @param nfilas 
 * @param ncols 
 */
void createFileFirstResult(char*** matrix,int nfilas, int ncols);
